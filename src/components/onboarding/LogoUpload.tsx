"use client";

import type React from "react";

// Intentionally removed logo upload UI.
// No logo, image, icon, or branding placeholders should appear in the component tree.

export function LogoUpload(): React.ReactElement {
  return <></>;
}

